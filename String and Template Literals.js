const firstName = 'hector'
const job = 'Developer'
const birthYear = 1993
const year = 2037

const hector = "I'm " + firstName + ', a ' + ( year - birthYear ) + ' years old ' + job + '!!'

console.log(hector)